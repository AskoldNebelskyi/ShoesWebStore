@model IEnumerable<ShoeNameIdDto>
@{
    ViewData["Title"] = "ManageShoes";
}
<h2>Manage Shoes</h2>
<br />
<div>
    <a asp-action="AddShoe" class="btn buy-now-btn">Add New Shoe</a>
</div>
<br />
<table class="table ">
    <thead>
        <tr>
            <th>Shoe Name</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var shoe in Model)
        {
            <tr id="shoe-@shoe.Id">
                <td>@shoe.Name</td>
                <td>
                    <a class="btn btn-link btn-no-effect" asp-action="EditShoe" asp-route-id="@shoe.Id">Edit </a>
                    |
                    <button class="btn btn-link btn-no-effect deleteBtn" data-id="@shoe.Id">Delete</button>
                </td>
            </tr>
        }
    </tbody>
</table>

@section scripts {
    <script>

        $(".deleteBtn").click((e) => {
            const id = $(e.target).data("id");
            if (!confirm("Are you sure to delete?"))
                return;

            $.ajax({
                url: '/admin/manageShoes/' + id,
                type: 'DELETE',
                success: () => {
                    const target = $("#shoe-" + id);
                    $(target).fadeOut(500, () => $(target).remove());
                },
                error: (e) => {
                    alert("Somthing Went Wrong", e);
                }
            });
        });

    </script>
}